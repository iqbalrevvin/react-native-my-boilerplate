import { Example, Home, Login, Startup } from '../screens';

const Screen = {
    STARTUP: {
        NAME: 'Startup',
        COMPONENT: Startup,
    },
    LOGIN: {
        NAME: 'Login',
        COMPONENT: Login,
    },
    HOME: {
        NAME: 'Home',
        COMPONENT: Home,
    },
    EXAMPLE: {
        NAME: 'Home',
        COMPONENT: Example,
    },
};

export default Screen;
