import { Example, Login, Startup } from '../screens';

const Screen = {
    STARTUP: {
        NAME: 'Startup',
        COMPONENT: Startup,
    },
    LOGIN: {
        NAME: 'Login',
        COMPONENT: Login,
    },
    EXAMPLE: {
        NAME: 'Home',
        COMPONENT: Example,
    },
};

export default Screen;
